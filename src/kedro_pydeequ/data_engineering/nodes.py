import pandas as pd
from pyspark.sql import DataFrame

def primary_sample(sample: DataFrame) -> DataFrame:
    """Preprocess the data for companies.

        Args:
            Sample: Source data.
        Returns:
            Preprocessed data.

    """

    return sample

